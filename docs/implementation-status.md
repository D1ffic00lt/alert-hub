# Implementation status

This page distinguishes code that exists in this repository from the complete distributed MVP
acceptance described by the product specification. Automated or controlled Docker evidence does
not prove the operator's real network, providers, runners, proxy, or iPhone.

The latest local backend verification completed 406 tests at 86.05% branch coverage, above the 75%
CI gate. Frontend lint/types, 70 unit tests, the production build, and fifteen Playwright paths also
passed. The repository retains separately recorded local passes for the two-image Docker matrix and
controlled three-node gate. These results become durable evidence for a reviewed commit only after
its GitHub checks are green.

## Implemented and repeatable in the repository

| Area                        | Current repository capability                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| --------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Runtime                     | Independent Python 3.12 API and static Nginx web images with separate `backend/` and `frontend/` build contexts. Both runtimes are unprivileged/read-only; only API mounts data/secrets, only web is host-published in split mode, API-only needs no Node/frontend, and the web proxy remains alive with fail-closed `503` behavior while API is unavailable.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Local persistence           | SQLAlchemy models, an explicit Alembic migration, SQLite WAL/foreign-key/busy-timeout initialization, short transactions, and verified online backup/restore tooling.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Authentication              | One-time bootstrap token, Argon2id password hashing with a dummy verification path, short access tokens, rotating hashed refresh sessions, revocation, exact Origin/CSRF/CORS policy, trusted-proxy resolution, and bounded per-node authentication limits.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Intake                      | Alertmanager, normalized generic JSON, and heartbeat endpoints with one-time source tokens stored only as keyed hashes, payload/rate limits, optional source CIDRs, and idempotent event keys. Each heartbeat request appends a replicated observation; connected nodes project the newest observation and reconcile missed/restored incidents even when observation and incident events arrive out of order.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Incidents                   | Deterministic cluster-wide incident identity, firing/resolved projection, acknowledge/resolve/silence/comments, out-of-order timelines, deduplication, audit events, and re-fire behavior.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Replication                 | Periodic peer pull over exact HTTPS origins, or literal RFC1918/ULA HTTP in an explicitly private-network mode, with bounded response/page sizes, persisted per-peer vector cursors, strict HTTP timeouts, independent backoff/jitter, relay of original origin/sequence, empty-cursor history bootstrap, tombstones, deterministic incident/heartbeat projections, delivery receipts, lag/clock metrics, and split-brain bootstrap detection that disables conflicting administrators. Receipt projection maps the sender's semantic `source_event_key` to the corresponding node-local incident event.                                                                                                                                                                                                                                                                                                                                                                                                 |
| Notifications               | Replicated channels/routes, encrypted provider configuration, matching and deterministic rendezvous ownership, durable outbox claims/retries, failover delay, delivery receipts, and adapters for Web Push, Telegram, SMTP, and generic webhooks. Web Push subscriptions are server-bound to authenticated sessions; logout, device revocation, session expiry, and owner disablement prevent delivery and replicate endpoint tombstones. Connected nodes derive ownership and delivery IDs from the logical incident-event key rather than a node-local row ID. SMTP supports bounded allowlisted `{{placeholder}}` subject/body templates with header-injection protection. Provider tests cover retry/permanent failure, escaping, timeouts, SSRF/redirect policy, redaction, and Web Push `404/410` cleanup.                                                                                                                                                                                         |
| Regional monitoring         | Replicated Prometheus datasources, AES-GCM encrypted bearer/basic credentials, connection tests, named backend-only PromQL, bounded SSRF-safe requests, and merged `probe_success` cells keyed by an explicit per-datasource label mode: canonical `source_region × target_name` by default or compatible `source_server × target_server`. Modes do not fall back into one another; partial-datasource errors remain visible. Administrators can save a replicated HTTPS Grafana link and bounded `job` globs for the named `up` summaries; the server constructs the selectors and never accepts browser-authored PromQL.                                                                                                                                                                                                                                                                                                                                                                               |
| Checks                      | Optional authenticated list, summary, and detail views over a protocol-neutral Check/Source/Target/Scenario/Variant model. All enabled Prometheus datasources are queried through seven fixed `synthetic_check_*` expressions at one evaluation time; normalization, five-state freshness/quorum aggregation, stable filters/pagination, alert association by exact `check_id`, safe Grafana deep links, combined cardinality limits, disabled/empty/unavailable responses, and the schema/UI handling for an explicitly stale envelope are implemented without a prober or SQLite time-series persistence. A bounded process-local cache coalesces refreshes and never presents a prior success as current after a required-source failure.                                                                                                                                                                                                                                                             |
| User interface              | Vite React SPA/PWA code is present for API-backed sources, channels, routes, datasources, incidents, checks, devices, cluster, audit, and settings workflows; a bounded seven-day overview of incident activity, response time, severity/source mix, and replicated delivery outcomes; memory-only access tokens; session-partitioned read caches; SSE with bounded runtime-telemetry polling; current peer health/lag instead of invented inventory health; short-burst audit grouping, progressive pagination, and full-field loaded-row export; trusted HTTPS read failover; isolated demo mode; and notification display/click handling. The local unit/build/Playwright gates pass; this is not real-device acceptance.                                                                                                                                                                                                                                                                             |
| Observability               | Live/ready/deep health with truthful local/peer/channel evidence, Prometheus metrics, structured UTC logs, request IDs, safe correlation fields, and configurable text/JSON log format and level.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| CI and supply chain         | The workflow runs on pull requests to `main` and pushes to `main`, with read-only GitHub-hosted jobs for repository style/policy, Ruff, strict mypy, branch-covered pytest, migrations, OpenAPI, frontend lint/types/tests/build/Playwright, dependency and secret audits, and Compose/recovery checks. After every cheaper gate succeeds, one ephemeral job builds the independent API/web images and exercises the exact compatible pair without transferring Docker archives through Actions artifacts. Actions are SHA-pinned, and superseded CI and CodeQL runs are cancelled. Normal CI never writes packages; version tags are owned by the separate release workflow, which reads the single product version from root `VERSION` and is the only workflow that publishes both exact tested `vX.Y.Z` images. Releases include per-image SBOM, GitHub-hosted provenance without synthetic GHCR tags, digest, compatibility, and GitHub Release metadata. A green run is still required per commit. |
| Controlled distributed test | A locally passing three-container, three-SQLite, non-root/read-only Docker scenario covers source/session replication, partitioned duplicate and reverse-order ingress, process loss, reconnection/restart, vector-cursor and projection convergence, re-fire, and connected-cluster single-owner notification delivery. It is a CI substitute, not proof of the real RU/NL/DE topology; the reviewed commit still needs its GitHub container job.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Deployment operations       | Default split, explicit split, and API-only Compose examples; ordinary public Nginx/Caddy examples that deny internal/operator paths plus dedicated TLS peer templates restricted to exact source `/32` and the health/query method-path pairs; fixed private edge-address host upstreams and container-service Caddy upstreams; separate manual deploy/rollback workflows; named RU/NL/DE Environments and static runner labels; a root-only node provisioner with validated narrow sudoers, rollback-safe locked activation, immutable topology policy, distinct loopback smoke ports, optional monitoring overlay, immutable-reference/compatibility validation, secret-file creation, smoke tests, component rollback, and release history. Production topology values remain outside Git.                                                                                                                                                                                                           |

## Required before distributed MVP acceptance

| Area                   | Remaining evidence or capability                                                                                                                                                                                                                                                                          |
| ---------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Real Web Push          | Exercise installation and user-gesture permission on a real iPhone Home Screen PWA; receive firing and resolved pushes while closed; verify deep-link click and reserve-node delivery after owner loss.                                                                                                   |
| Real providers         | Test configured Web Push, Telegram, SMTP, and webhook accounts/endpoints from the intended egress network, including credentials, rate limits, provider responses, and ownership failover. Mocks do not prove external delivery.                                                                          |
| Production topology    | Inventory and validate actual peer DNS/TLS, direct source-address preservation, exact `/32` proxy/application allowlists, firewall and public exposure, clock state, Prometheus labels/auth, Checks metric inventory/cardinality and safe aliases, Grafana links, and existing Nginx/Caddy configuration. |
| Failure exercises      | Attach evidence for real one/two-node loss, RU↔EU partition/recovery, delivery-owner loss, runner offline, failed migration/readiness with automatic rollback, invalid proxy candidate restoration, manual `previous` rollback, and scheduled backup restore.                                             |
| Replication lifecycle  | Design compact authenticated snapshot/bootstrap and tombstone/history retention, plus an authenticated operator workflow for resolving a detected bootstrap split brain. Empty-cursor full-history replay works but is not compaction.                                                                    |
| Secret lifecycle       | Add and exercise bulk master-key re-encryption/rotation and an encrypted offline key-recovery procedure. Database backup alone cannot recover encrypted provider/subscription values.                                                                                                                     |
| Browser hardening      | Add installation-level CSP reporting and replace the remaining narrowly scoped inline-style allowance with a nonce/hash or equivalent design after validating the production proxy/runtime integration.                                                                                                   |
| Release infrastructure | Configure branch protection, protected tags/environments, repository-scoped self-hosted runners and root-owned wrappers on the real nodes; then exercise a protected release and ordered deployment.                                                                                                      |

The detailed register is in [acceptance evidence](acceptance-evidence.md). Do not describe a
release as the distributed MVP until every applicable external row has dated evidence and the
real-iPhone Web Push flow has passed.

Database migrations remain forward-only and compatible with application version N-1. Ordinary
application rollback never downgrades or restores SQLite; database restore is a separately
confirmed disaster-recovery operation.
